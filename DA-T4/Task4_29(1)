#include <iostream>
using namespace std;

long long gcd_recursive(long long a, long long b) {
    if (a == 0 && b == 0)
        return -1; 

    if (b == 0)
        return llabs(a);

    return gcd_recursive(b, a % b);
}

int main() {
    long long a, b;
    cin >> a >> b;

    long long result = gcd_recursive(a, b);

    if (result == -1)
        cout << "GCD is undefined\n";
    else
        cout << "GCD = " << result << endl;

    return 0;
}

